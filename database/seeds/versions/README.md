# Seed which creates cross section sets which have versions and is retracted.

 Load with

 ```shell
 # To setup user and org
 npm run seed seeds/test
 # To create versioned CrossSectionSet
 npm run seed seeds/versions
 ```
