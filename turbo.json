{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "format": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist/**", ".next/**", "target/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "target/**"]
    },
    "start": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "test": {
      "outputs": []
    },
    "annotate": {
      "outputs": []
    }
  }
}
