{
  "name": "@lxcat/schema",
  "version": "0.1.0",
  "description": "Schema and validator for LXCat",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "json:css": "ts-json-schema-generator -f ./tsconfig.json --no-top-ref --path src/css/input.ts --type CrossSectionSetRaw --out src/css/CrossSectionSetRaw.schema.json -e none",
    "json": "pnpm json:css",
    "build": "tsc",
    "lint": "eslint src/ --ext .ts",
    "format": "prettier --write src/",
    "test": "vitest",
    "dev": "tsc --watch",
    "addheader": "reuse addheader --license=Apache-2.0 --copyright='LXCat team' -r --skip-existing --exclude-year src",
    "prepublishOnly": "pnpm build",
    "clean": "rm -rf dist"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "ajv": "^8.12.0",
    "ts-json-schema-generator": "^1.2.0"
  },
  "devDependencies": {
    "@lxcat/tsconfig": "^0.1.0",
    "@typescript-eslint/eslint-plugin": "^5.54.0",
    "c8": "^7.13.0",
    "eslint": "8.10.0",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5",
    "vitest": "^0.15.2"
  },
  "main": "dist/css/validate.js",
  "types": "dist/css/validate.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
